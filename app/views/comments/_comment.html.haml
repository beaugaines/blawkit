= div_for(comment, class: 'comment') do
  = link_to '', class: 'pull-left' do
    = image_tag(comment.user.avatar.small.url)

  %p= "Posted by #{comment.username} #{time_ago_in_words(comment.created_at)} ago"
  = markdown comment.body

