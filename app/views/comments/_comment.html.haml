= div_for(comment) do
  = link_to '#', class: 'pull-left avatar' do
    = show_user_avatar(comment.user, '80x80')

  %p 
    = "Posted by #{comment.username} #{time_ago_in_words(comment.created_at)} ago"
    - if can? :destroy, comment
      %small= link_to 'Delete', [comment.post, comment], method: :delete, remote: true
  %p= markdown comment.body

